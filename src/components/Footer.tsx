import React from "react";

//footer

const Footer = (props: { basic?: boolean }) => {
  return (
    <header>
      <div>
        <h1>COM STORE</h1>
      </div>
      <nav>
        <ul>
          <li>
            <a href="/">Contact us</a>
          </li>
        </ul>
      </nav>
    </header>
  );
};

export default Footer;
